name: AUTO-CI-ZAP-DAST-ANALYSIS

on:
  push:
    branches:
      - main
      - development
      - preproduction

jobs:
  zap_scan:
    name: OWASP ZAP Full API Scan
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Docker
        uses: docker/setup-buildx-action@v2

      - name: Build and Run API Container
        run: |
          echo "Construyendo la imagen de la API..."
          docker-compose up --build -d

      - name: Wait for API to be ready
        run: |
          echo "Esperando 60 segundos para que la API esté lista..."
          sleep 60

      - name: Download OpenAPI Specification (si está disponible)
        run: |
          curl -o openapi.json http://localhost:10000/openapi.json
          continue-on-error: true

      - name: Run ZAP API Scan
        uses: zaproxy/action-api-scan@v0.9.0
        with:
          target: "http://securemail_container:10000"
          openapi: "openapi.json"
          fail_action: false
          format: "json"
          output_file: "zap_report.json"

      - name: Upload ZAP Report
        uses: actions/upload-artifact@v4
        with:
          name: zap-report
          path: zap_report.json

      - name: Stop and Remove Containers
        if: always()
        run: |
          echo "Deteniendo contenedores..."
          docker-compose down
